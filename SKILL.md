---
name: auto-handoff
description: 自动管理长任务会话交接。1、用户表达持续工作意向（"任务较大/耗时较久/一直做不要停/持续工作"、或设置 /goal 目标如"创建/goal目标持续工作""开个长期目标"）时加载执行，开启自主监测并在合适时机自动交接；2、用户表示"会话交接/开新会话"时直接执行交接，即便用户先前表示不要交接；3、用户表达接近持续工作或交接但意图模糊时，先确认询问，若用户未响应（超时）默认视为确认；4、用户表示"不要切换会话/不要交接"时，禁止自动执行本技能，但用户又明确表示"立即交接"时仍执行。
---

# 会话交接（Session Handoff）

## 触发执行

满足以下任一条件时，执行本技能：

1. 持续工作授权（明确）：用户明确表达了"任务可能较大"、"任务耗时可能较久"、"一直做不要停"、"持续工作"、"创建/goal目标持续工作"等长期跟进意向，或主动设置 /goal 目标。加载技能，立即开启自主监测。
2. 明确交接指令：用户表达了"会话交接"、"handoff"、"开新会话"、"换个会话继续"等。加载技能并立即执行交接。
3. 模糊交接意向：用户意向接近第二条，但较为模糊，并未明确授权时 → 先确认："您是想交接到新会话继续吗？"用户确认后立即执行交接。
4. 持续工作模糊意向：用户表达了接近"持续工作"但不够明确的意图（如"挂个任务跑着"、"帮我盯着这个项目推进完"、"建个 goal 慢慢搞"），或设置了 /goal 目标但未明说是否持续监测时 → **调用问答工具（question / ask_user）确认**，不要只用手写文字询问：

   - 问题："您是要开启持续工作模式吗？我会自动监测上下文占用率，在适当时机生成交接摘要并开新会话接手。"
   - 选项："开启持续工作模式（推荐）" / "不开启，只做当前任务"
   - **超时处理**：问答工具设置阻塞超时（约 60 秒），超时无响应（用户已离开 / 设 goal 后走人）→ 默认视为"开启"，立即进入自主监测，不再询问。
   - 用户明确选择"不开启" → 不开启。
   - 降级：若问答工具不可用，用文字询问；此时以"本会话再次被拉起时用户仍未回应"作为超时信号，默认开启。

如果用户说过"别切换"或"本会话不要交接"，本会话内停止一切自主交接（含持续工作模式的自主监测），只有当用户明确说"交接"时才执行。

## 场景A：持续工作模式（自主交接）

用户授权"持续工作"后（明确授权，或经触发条件第 4 条确认 / 超时默认开启），LLM进入自主监测状态：

1. 继续正常对话，完成各项工作任务。
2. 当每个小任务收尾时（即到达段落边界），执行一次上下文占用查询，非段落边界停止执行本技能。

```
powershell -NoProfile -ExecutionPolicy Bypass -File "scripts\calc_ctx_use_rate.ps1" -SessionId <当前会话ID>
```

3. 查询结果处理：
   - 占用率 ≥ 45%：**立即执行交接摘要生成**（不需要问用户，直接交接）
   - 占用率 ≥ 25%：当前任务已产出阶段性成果（如所有目标已完成、代码已提交、测试已通过），且接下来要做的事需与本会话任务关联性不大或需要读取大量新内容（新文件、新模块、新领域）时，执行交接，否则继续工作
   - 占用率 < 25%：继续工作，不交接
   - 脚本执行失败（failback）：
     在生成回复的过程中，观察并记录以下可量化指标：

     1. 本回复中是否明确引用了早期（对话前半段）的用户指令或事实
     2. 本回复是否需要向用户追问“你刚才说的 XX 是指什么”之类的澄清（说明上下文已模糊）
     3. 本回复中是否出现了“如前所述”“之前提到过”“正如你之前所说”等回溯性措辞

     满足任意 1 条 → 立即执行交接摘要生成
     0 条 → 继续工作，并在回复末尾告知用户：“上下文统计暂时不可用，但本次未触发交接。”

当前任务已产出可交付成果（代码合并、文档定稿、Bug关闭），且下一任务与上一任务无明显上下文依赖。

4. 交接完成后，本会话结束。

## 场景B：用户发起（直接交接）

B1：明确指令
用户表达意向："交接" / "接力" / "handoff" / "开新会话" / "换个会话继续" / "新会话接手"等。
→ **直接执行交接摘要生成**，不确认，不询问。

B2：模糊意向
识别到用户意图可能为B1，但又不明确时 → 先确认："您是想交接到新会话继续吗？" 用户确认后执行交接摘要生成。

## 交接摘要模板

```markdown
【会话交接摘要】

## 当前状态
[自然语言描述项目/任务当前在什么位置。示例："订单模块的重构进行到一半。`src/order.py` 的 `create_order` 已改完，`process_payment` 还没动。迁移文件已写好但未执行。"]

## 已确认的事实（无需重新探索）
- [架构决策：如"支付网关统一用 `payment_gateway.process()` 接口"]
- [已验证结论：如"数据库连接池已从 5 扩到 20，性能达标"]
- [已确认的约束：如"Python 3.10，必须使用类型注解"]
- [已明确的项目结构/文件位置列表：如"支付页面在`app/payment/`下，后端接口 `server/apis.py`，核心逻辑在 core/"]

## 待处理的工作
- [方向性描述，不限定具体做法。示例："`process_payment` 需要补上，找个合适的方式集成支付网关"]
- [示例："开始开发用户管理，在后台新增用户管理菜单，提供增删改查管理功能，用户名不可重复，密码必须12位以上。可导入、导出。"]

## 已知的坑
- [已经踩过的报错/依赖/权限问题，新会话不用再踩一遍]
- [没有就写"无"]

## 会话目标 
[用/goal工具在新会话重建，目标文本照抄旧目标原文，可根据新会话任务酌情调整，但尽量不要删减；没有则删去本段]

## 会话级状态
  - [任务状态：todos（未完成/进行中/已完成标题）]
  - [计划文件：`.omo/plans/` 相关计划]
```

## 交接摘要填写规则：
- "当前状态"用自然语言描述项目整体情况，让新会话快速建立认知。
- "已确认的事实"是旧会话花Token搞清楚的东西，主要是与新会话任务相关的内容，直接固化下来，避免新会话盲目查找耗时耗力。
- "待处理的工作"只描述方向，尽量不限定具体做法，给新会话留判断空间，除非是已经过设计的比较明确的内容。
- "已知的坑"帮助新会话避开已经踩过的雷，包括项目内的，环境上的，等各种已知的坑。
- "会话目标"使用/goal命令查询本会话的目标，如果有并且处于激活状态，则添加本段。
- "会话级状态"如果有未完成任务时才需要添加，如果没有则整段删除。
- 所有内容从当前可见对话中提取，禁止编造。

## 交接步骤
开新会话并注入交接摘要
- `session.create`：directory 同本会话，title 带任务关键词；
- `session.send` 将交接摘要一次发完
如果工具不可用，将交接摘要输出给用户，由用户手动开新会话粘贴。

## 交接后（清理顺序不可颠倒：先清理系统状态，最后才输出结束语）

> ⚠️ 交接不是口头承诺。只要系统（goal、todo、continuation、定时任务）还认为本会话"有未完成工作"，就会反复拉起本会话继续工作，与新会话争抢同一批文件。必须把系统状态清理到与"已交接"一致。**所有清理动作做完之前，禁止输出结束语**（一旦先输出结束语，就容易提前停手、跳过清理）。

按以下顺序逐步执行，每步都是真实工具调用，禁止合并、禁止跳过：

1. **停止所有 continuation 机制**：执行 `/stop-continuation`（停 ralph loop / todo continuation / boulder）。必须最先做，防止清理过程中系统又自动拉起本会话。

2. **清空 todo 列表**：调用 `todowrite`，把全部 todo 标记为 `completed`；已交接给新会话、本会话不再做的项，标记 `cancelled` 并注明"已交接"。**绝不能留下任何 pending / in_progress**——这是系统拉起本会话的头号触发源。

3. **关闭 active goal**：
   - 先 `get_goal` 确认本会话是否有 active goal。
   - 若有，调用 `update_goal(status="complete")` 关闭。**不要传 `session_id` 参数**（缺省即当前会话，最可靠）。误传其他会话 ID（尤其是 openchamber 新会话的 ID）会导致 goal 关不掉、旧会话被 goal 机制反复拉起——这正是"切换后 goal 没清理、旧会话被拉起又继续工作"最常见的根因。
   - 关闭后再次 `get_goal` 验证：返回 null，或 status 已不是 active。
   - 若 `update_goal` 失败或无法确认，执行 `/goal clear` 命令兜底。

4. **停用/移除自动化任务**：若本会话由定时任务（schedule）拉起，执行 `schedule.toggle(disabled=true)` 或 `schedule.delete` 移除，防止定时再次拉起。

5. **验证（必须）**：重新 `get_goal` 确认无 active goal；重新 `todowrite` 确认无 pending/in_progress。两者都通过后才能进入下一步。

6. **最后**才输出："✅ 本会话已交接。本会话结束。"

## 交接后的防御（防止被拉起后继续工作）

交接完成即代表本会话**永久终止**。此后无论以何种方式被拉起（用户新消息、goal/continuation 机制、定时任务），本会话一律：

1. **拒绝工作**：直接回复"本会话已交接并结束，请在新会话中继续。"——不读文件、不改文件、不建 todo、不执行任何任务。
2. **再次清理**：重复执行上节"交接后"的第 1~5 步（stop-continuation → 清 todo → 关 goal → 停 schedule → 验证），确保状态彻底干净。
3. **立即停止**：清理完成后不再做任何事。

判定"本会话已交接"的依据（满足任一即成立）：
- `get_goal` 返回的 goal 已是 complete，或返回 null 且本会话历史上曾设置过 goal；
- 对话历史中本会话已输出过"✅ 本会话已交接。本会话结束。";
- todo 全部为 completed/cancelled 且无 active goal。

## 禁止事项

- 禁止在非段落边界执行上下文查询。
- 禁止在占用率 < 25% 时执行自主交接。
- 禁止在摘要中列出详细的操作细节（如“先如何改A，再如何做B”）——只传递当前状态和已确认的事实，具体怎么做由新会话自行判断。
- 禁止在系统状态清理完成（stop-continuation + 清 todo + 关 goal 验证通过）之前输出"本会话已结束"。
- 已交接的会话被拉起时，禁止继续任何工作，只能拒绝并再次清理。